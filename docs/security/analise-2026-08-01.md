# Análise de Segurança — O Mestre Afiliado

**Data:** 2026-08-01  
**Escopo:** API (`apps/api/src/`), autenticação, rotas expostas, CORS, webhooks  
**Classificação:** Confidencial

---

## Legenda

| Ícone | Severidade                                          |
| ----- | --------------------------------------------------- |
| 🔴    | CRÍTICA — explorável remotamente, impacto imediato  |
| 🟠    | ALTA — impacto significativo, exploração facilitada |
| 🟡    | MÉDIA — risco real porém com pré-condições          |
| 🔵    | BAIXA/INFO — observação ou item já mitigado         |

---

## 🔴 [CRÍTICA] #1 — Rotas ML sem nenhuma autenticação ✅ CORRIGIDO

**Arquivo:** `apps/api/src/modules/ml/ml.routes.ts`  
**Status:** ✅ Corrigido em 2026-08-01

As seguintes rotas **não tinham middleware JWT nem verificação de auth**:

| Método   | Rota                                            | Risco                                                           |
| -------- | ----------------------------------------------- | --------------------------------------------------------------- |
| `PUT`    | `/api/ml/affiliates/:mlUserId`                  | Sobrescrever sessionCookies/meliid/melitat de qualquer afiliado |
| `DELETE` | `/api/ml/affiliates/:mlUserId`                  | Deletar qualquer afiliado                                       |
| `POST`   | `/api/ml/convert`                               | Uso indevido de credenciais de afiliados alheios                |
| `POST`   | `/api/ml/refresh`                               | Refresh de token OAuth de terceiros                             |
| `POST`   | `/api/ml/affiliates/:mlUserId/validate-cookies` | Testar cookies de sessão de qualquer afiliado                   |
| `GET`    | `/api/ml/affiliates`                            | Listar TODOS os afiliados com tokens e cookies visíveis         |

**Correção aplicada:**

- Adicionado `.use(createJwtPlugin())` no módulo
- Todas as rotas agora exigem autenticação JWT
- Verificação de ownership: `affiliate.userId === auth.userId`
- Listagem filtra por `userId: auth.userId` (admin vê todos)
- Rotas OAuth (`/api/ml/auth` e `/api/ml/callback`) mantidas públicas por design

**Testes:** ✅ 462 testes passaram, 0 regressões

---

## 🔴 [CRÍTICA] #2 — IDOR nos Mirrors (sem checagem de ownership) ✅ CORRIGIDO

**Arquivo:** `apps/api/src/modules/mirrors/mirrors.routes.ts`  
**Status:** ✅ Corrigido em 2026-08-01

| Método   | Rota                      | Problema                                 |
| -------- | ------------------------- | ---------------------------------------- |
| `GET`    | `/api/mirrors/:id`        | Lia mirror sem verificar `userId`        |
| `PUT`    | `/api/mirrors/:id`        | Atualizava mirror sem verificar `userId` |
| `PATCH`  | `/api/mirrors/:id/status` | Alterava status sem verificar `userId`   |
| `DELETE` | `/api/mirrors/:id`        | Excluía mirror sem verificar `userId`    |

**Correção aplicada:**

- Todas as rotas agora verificam `mirror.userId === auth.userId` ANTES de qualquer operação
- `GET /api/mirrors/:id` — retorna 404 se não for dono
- `PUT /api/mirrors/:id` — verifica ownership antes de buscar sourceGroups antigos
- `PATCH /api/mirrors/:id/status` — verifica ownership ANTES de chamar `patchStatus()` (evita modificação indevida)
- `DELETE /api/mirrors/:id` — verifica ownership antes de deletar

**Testes:** ✅ 462 testes passaram, 0 regressões

````

---

## 🔴 [CRÍTICA] #3 — Webhook sem autenticação

**Arquivo:** `apps/api/src/modules/webhook/webhook.routes.ts:285`

```typescript
void EVOLUTION_API_KEY;
````

A variável `EVOLUTION_API_KEY` é lida (linha 28) mas **nunca é verificada** no handler. Qualquer pessoa pode enviar `POST /webhook/message` e:

- Injetar mensagens falsas na Queue A do pipeline de espelhamento
- Alterar status de conexões WhatsApp de qualquer usuário
- Invalidar caches de grupo

O header `apikey` que a Evolution API envia é completamente ignorado.

**Correção sugerida:**

```typescript
const providedKey = request.headers.get('apikey');
if (!EVOLUTION_API_KEY || providedKey !== EVOLUTION_API_KEY) {
  set.status = 401;
  return { success: false, error: 'Unauthorized' };
}
```

---

## 🔴 [CRÍTICA] #4 — JWT Secret hardcoded como fallback

**Arquivo:** `apps/api/src/middleware/auth.ts:31`

```typescript
secret: config.JWT_SECRET || 'omestre-dev-secret-change-in-production',
```

Se `JWT_SECRET` não estiver no `.env` (ou estiver vazio), o sistema usa uma string conhecida. Tokens forjados com esse secret dão acesso admin (`isAdmin: true` no payload).

`config.ts` (linha 14) define `JWT_SECRET` sem default, então o fallback nunca deveria ativar — **mas se alguém deployar sem `.env` ou com a var vazia, é game over.**

**Correção sugerida:**

- Remover o fallback e lançar erro no startup se `JWT_SECRET` estiver vazio.
- Alternativamente, gerar um secret aleatório em dev e logar aviso.

---

## 🟠 [ALTA] #5 — CORS completamente aberto

**Arquivo:** `apps/api/src/index.ts:40`

```typescript
.use(cors())
```

Sem configuração = permite qualquer origin. Em produção, qualquer site pode fazer requests autenticados. Embora o frontend use Bearer token (não cookies), CORS aberto permite:

- Data exfiltration via JavaScript de terceiros se o token vazar
- Enumeração de endpoints e fingerprinting da API

**Correção sugerida:**

```typescript
.use(cors({
  origin: [config.FRONTEND_URL],
  methods: ['GET', 'POST', 'PUT', 'PATCH', 'DELETE'],
}))
```

---

## 🟠 [ALTA] #6 — JWT sem expiração

**Arquivo:** `apps/api/src/middleware/auth.ts:32-36`

O schema do JWT não inclui `exp`. Tokens **nunca expiram**. Se um token vazar, o atacante tem acesso permanente até que o secret seja trocado.

**Correção sugerida:**

```typescript
const token = await jwt.sign({
  userId: user.id,
  userEmail: user.email,
  isAdmin,
  exp: Math.floor(Date.now() / 1000) + 7 * 24 * 60 * 60, // 7 dias
});
```

Adicionar `exp` ao schema e revalidar no login/refresh.

---

## 🟠 [ALTA] #7 — Sem rate limit em login/register

**Arquivo:** `apps/api/src/modules/auth/auth.routes.ts`

| Rota                      | Linha | Problema                                   |
| ------------------------- | ----- | ------------------------------------------ |
| `POST /api/auth/register` | 14    | Sem rate limit — criação massiva de contas |
| `POST /api/auth/login`    | 59    | Sem rate limit — brute force de senhas     |

Só a extensão Chrome tem rate limit (`extension-log.routes.ts`).

**Correção sugerida:**

- Rate limit por IP: 5 tentativas de login por minuto, 3 registros por hora.
- Bloquear IP após N falhas consecutivas.

---

## 🟠 [ALTA] #8 — Swagger exposto em produção

**Arquivo:** `apps/api/src/index.ts:42-52`

```typescript
swagger({ path: '/docs', ... })
```

Sem restrição de ambiente. Documentação completa da API (com schemas e descrições) acessível publicamente em produção.

**Correção sugerida:**

```typescript
if (process.env.NODE_ENV !== 'production') {
  .use(swagger({ path: '/docs', ... }))
}
```

Ou proteger com auth admin.

---

## 🟡 [MÉDIA] #9 — POST /api/convert sem autenticação

**Arquivo:** `apps/api/src/index.ts:140-172`

Rota pública que usa credenciais do `.env` (`SHOPEE_APP_ID`, `SHOPEE_SECRET`, ML tokens) para converter URLs. Qualquer pessoa na internet pode usar suas credenciais de afiliado gratuitamente.

**Correção sugerida:**

- Adicionar autenticação JWT.
- Ou remover a rota (é um legacy do pré-multi-tenant).

---

## 🟡 [MÉDIA] #10 — SSRF em ML convert (meli.la resolver)

**Arquivo:** `apps/api/src/modules/ml/ml.routes.ts:302`

```typescript
const resolved = await fetch(url, { method: 'HEAD', redirect: 'manual' });
```

O URL vem direto do body do usuário. Se o atacante enviar `http://169.254.169.254/latest/meta-data/` (AWS metadata) ou `http://localhost:5443/` (PostgreSQL), o servidor faz o request. Sem allowlist de domínios.

**Correção sugerida:**

- Validar que a URL pertence a `mercadolivre.com.br`, `mercadolibre.com`, ou `meli.la` antes de fazer fetch.

---

## 🟡 [MÉDIA] #11 — OAuth state sem CSRF protection

**Arquivo:** `apps/api/src/modules/ml/ml.routes.ts:39`

```typescript
const state = (query as { userId?: string }).userId || '';
```

O parâmetro `state` do OAuth é apenas o `userId` em plain text. Sem nonce aleatório, sem HMAC. Um atacante pode forjar o callback e vincular uma conta ML arbitrária a qualquer `userId`.

**Correção sugerida:**

- Gerar nonce aleatório + HMAC do userId.
- Validar no callback.
- Armazenar nonce em Redis com TTL curto (5 min).

---

## 🟡 [MÉDIA] #12 — GET /api/mirrors/:id expõe dados cross-tenant

**Arquivo:** `apps/api/src/modules/mirrors/mirrors.routes.ts:136`

Mesmo sem IDOR write (#2), o GET expõe `sourceGroups`, `targetGroups` e `messageTemplate` de mirrors alheios — dados que incluem JIDs de grupos WhatsApp privados.

**Resolução:** Coberta pelo fix do **#2** (ownership check).

---

## 🔵 [BAIXA] #13 — Extension logs: API key comparison ✓

**Arquivo:** `apps/api/src/modules/extension/extension-log.routes.ts:26`

`safeEqual()` está implementado corretamente (constant-time). Se `EXTENSION_LOGS_API_KEY` estiver vazia no `.env`, o endpoint rejeita tudo (fail-closed).

**Status:** ✅ Já seguro.

---

## 🔵 [BAIXA] #14 — Magalu slug validation ✓

**Arquivo:** `apps/api/src/modules/magalu/magalu.routes.ts`

Validação regex `^[a-z0-9-]{3,40}$` no slug previne injection no fetch de validação (`magazinevoce.com.br/{slug}/`).

**Status:** ✅ Já seguro.

---

## Resumo por prioridade

### URGENTE (corrigir imediatamente)

| #   | Vulnerabilidade     | Fix                                    |
| --- | ------------------- | -------------------------------------- |
| #1  | Rotas ML sem auth   | Adicionar JWT + ownership check        |
| #2  | IDOR nos Mirrors    | Checar `mirror.userId === auth.userId` |
| #3  | Webhook sem auth    | Validar header `apikey`                |
| #4  | JWT secret fallback | Remover fallback, crashar se vazio     |

### IMPORTANTE (corrigir esta semana)

| #   | Vulnerabilidade      | Fix                      |
| --- | -------------------- | ------------------------ |
| #5  | CORS aberto          | Restringir origins       |
| #6  | JWT sem expiração    | Adicionar `exp` (7 dias) |
| #7  | Sem rate limit login | Adicionar rate limiter   |
| #8  | Swagger exposto      | Desabilitar em produção  |

### DESEJÁVEL (corrigir em breve)

| #   | Vulnerabilidade            | Fix                       |
| --- | -------------------------- | ------------------------- |
| #9  | `/api/convert` sem auth    | Adicionar auth ou remover |
| #10 | SSRF no ML convert         | Allowlist de domínios     |
| #11 | OAuth state fraco          | Nonce aleatório + HMAC    |
| #12 | Mirror detail cross-tenant | Coberto pelo fix do #2    |

---

## Revision history

| Data       | Versão | Mudança         | Motivo                            |
| ---------- | ------ | --------------- | --------------------------------- |
| 2026-08-01 | 1.0    | Análise inicial | Varredura de segurança pré-deploy |
