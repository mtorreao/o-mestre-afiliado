/**
 * Amazon Service — Repositório singleton + helpers compartilhados
 * para as rotas de Amazon.
 */
import { AmazonAffiliateRepository } from '@omestre/db';

export const amazonRepo = new AmazonAffiliateRepository();
